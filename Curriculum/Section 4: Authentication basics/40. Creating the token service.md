We're going to our program class because this is where register our services.

Now in order to register this as a service we say builder not services. And then we add one of three different lifetimes for this service.

- AddSingleton - when our application starts up it will create an instance of our service, and it will keop it running for as long as the application is alive.

- AddTransient - create a new instance of our token service for every single request. 

And realistically this one's considered too short for our purposses, the one that we typically use for a service thats goint to be used inside an API contoler is

- AddScroped - it's going to be scoped to the level of the HTTP request. So our request comes into our account contoller. we're gonna inject the token service into our account contoller. And it will be created when a request comes in. And it'll be disposed of when the HTTP request in finished.