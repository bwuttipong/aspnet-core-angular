@AppUser.cs

please make sure you're inside your API folder

```bash
dotnet ef migrations add UserEntityUpdated
```

So, lets see if we can apply this migration to our database

```bash
dotnet ef database update
```