But we also get the token.
And what we have here is a JSON web token.
And what we can do is go and take a look at what's inside this token because most of the token is easily decoded, most of the token is not encrypted.
We'll go across to the browser and let's open up a new tab and we'll just go to a website called

```mardown
jwt.io
```