 And now let's take a look at making an HTTP request. And to accomplish this we need to understand a little bit about the component lifecycle in angular. but before we get to that, we need to make the HTTP client module avaliable to our anglaur app, because is'that HTTP client module that we're going to be using to make HTTP requests. But we need to privide this to our application as a whole.

 And when need to provinde someting to our application as a whole, we use our app config.

 we have to implement its methods and we get a warning telling us that propery ng on init missing

 ### Summary

 This lesson wraps up Section Three of the course, focusing on the significant learning goals achieved. Here are the main points:

    1. Completion of the Walking Skeleton: The primary goal was to finalize the foundational structure of the Angular application, referred to as the "walking skeleton."

    2. Introduction to Angular CLI: Students gained an introductory understanding of using the Angular Command Line Interface (CLI) for creating and managing Angular applications.

    3. Understanding Project Files: The lecture highlighted the main project files needed in an Angular application, emphasizing how components and files work together, particularly the necessity of at least one app module (app.module.ts) for bootstrapping the root app component.

    4. Using HTTP Client Service: The lesson covered how to utilize the HTTP client service for fetching data from APIs and displaying it in the application.

    5. HTTP to HTTPS Transition: There was a switch from running the application over HTTP to HTTPS to enhance security.

    6. Package Management with npm: The students learned how to add packages using npm to extend the application’s functionality.

    6. Next Steps: The lesson concluded with a preview of the next topic, which will focus on implementing authentication in the application.

This summary encapsulates the learning and progress made through this section, setting the stage for upcoming lessons.