And we can add an extension or two to help us out when we're writeing the code.
so, let's go to the extension tab. And first of all, we'll just search for angualr.

- Angular Language Service
- ESLint (we'll utilize that's very helplful) which may or may not be necessary because i can see the app.ts already shouring at me

it's a setting that we need enable. So just to to settings and click on settings and just search for brackets.

But I'm gonna change that to alwas because in our HTML templates obviously it doesn't know that.

And then we can specify title And we do get some autucompleteion help inside our templates as well.

Another thing that i'd like to change inside here.

Supposing I wantedthis to be an H2 instead, it doesn't automatically update the closing tag at the same time, so that's another setting that we can enable inside VS code as well.

and we need to search for linked

### Summary

This lecture provides an overview of the structure of an Angular project and how to run it using the Angular CLI. Key points include:

Running the Application: The instructor demonstrates how to navigate to the client folder and use the command 'ng serve' to run the application, which can be accessed at 'http://localhost:4200'.

Project Structure: The project is introduced with a focus on the single index.html file in the source folder, serving as the application's entry point. The HTML contains an Angular component '<app-root>', defined in the app component file (app.ts).

Component Naming Convention: There's a discussion on the new naming convention for components, which simplifies the file names by removing the '.component' suffix, prompting some debate among developers.

Root Component: The app component is identified as the root component with a decorator that provides metadata for processing. Components are highlighted as the fundamental building blocks of Angular apps.

Dynamic Content: The lecture explains how to use interpolation to display dynamic content, like a title property, in the HTML template.

Hot Module Reloading: It covers hot module reloading, which allows real-time code changes to be reflected in the browser, improving the development experience.

Bootstrap File: The main.ts file is introduced as the bootstrap file, responsible for initializing the Angular app and rendering the root component.

Standalone Components: The move towards standalone components is noted, moving away from the traditional modular approach.

Configuration Files: The app.config file is mentioned for application settings, while clarifying that script tags are absent in index.html, as the main.js file is dynamically injected.

Important Files: Other significant files like styles.css, .gitignore, and angular.json are briefly discussed, with emphasis on maintaining a clean project structure as the focus shifts to creating components, each with their own files.

The next lesson will cover VSCode extensions relevant to Angular development.

Was this content relevant to you?
summerize this lession

This lesson concludes Section Three, focusing on the learning goals achieved throughout the project. Here are the key takeaways:

    1. Completion of the Walking Skeleton: The primary goal was to finalize the "walking skeleton," establishing a foundational Angular application.

    2. Understanding Angular CLI: It provided an introductory overview of using the Angular Command Line Interface to create and manage Angular applications.

    3. Project Structure Familiarization: The lesson included an explanation of the main project files involved in Angular, particularly how components and files work together. You learned that at least one application module (app.module.ts) is necessary for bootstrapping the root app component.

    4. HTTP Client Service: The use of the HTTP client service was covered, which allows you to fetch data from APIs and display it in the browser.

    5. Switch from HTTP to HTTPS: The application was transitioned from running on HTTP to HTTPS for enhanced security.

    6. Package Management: It included a walkthrough on adding packages using npm to extend the functionality of your application.

    7. Next Steps: The lesson concludes by introducing the upcoming topic of authentication and how to integrate it into the application.

This summary encapsulates the learning objectives and progress made during this section of the course.