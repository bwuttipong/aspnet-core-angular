so we need to turn off this rule because there's nothing we can do about it.

styles.css
```css
Unknown at rule @plugin css(unknownAtRules)
@plugin "daisyui";
```

And i'm just gonna search for in the settings, as this is very specifically a vscode issue.

i'm just gonna search for unknow at rules (unknownAtRules). we're going to ignore it. 

**CSS** > Lint: **Unknow At Rules**
Unknow at-rule.
```markdown
ignore
```

And something else that i want to mention as wel is at he moment. 
we don't get much help with tailwind.

If i start typing someting inside here, i get the previous styles
that i've used, but if i wanted a front based style, typically it starts with font dash, but i don't get any intellisense at the momoent, so we can add an extension to help us with this.

And what we'll look for inside extensions is **tailwind**.

```markdown
Tailwind CSS IntelliSense
```

### Summary

The lecture titled "Using the new styling tools in our app" focuses on enhancing the layout of an application using Daisy UI and Tailwind CSS. It starts by addressing a CSS ESLint warning in VSCode regarding unknown at rules, and teaches how to disable it for uninterrupted styling.

The importance of Tailwind CSS IntelliSense is highlighted, including the installation of its extension for better coding assistance. The speaker then demonstrates creating a visually appealing card component with Daisy UI, discussing the use of Tailwind's utility classes for background colors, widths, margins, and padding. Understanding the rem units in Tailwind is emphasized.

Additionally, the lecture showcases how to incorporate images and lists into the card component, managing assets within the public folder of the Angular application for correct image access without needing to specify the public directory. The session concludes with a preview of next steps, such as running the Angular application over HTTPS, setting the stage for a fully functional application.

If you need more details or have specific questions, feel free to ask!

Was this content relevant to you?
