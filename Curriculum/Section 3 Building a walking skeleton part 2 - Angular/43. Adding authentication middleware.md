So i'm gonna open up the NuGet gallery once again.
And the package that we need for this one is Micorsoft.

```markdown
Microsoft.AspNetCore.Authentication.JwtBearer
```

So we need to add authentiation as a service at Program.cs

```csharp
builder.Services.AddAuthentication(JwtBearerDefaults);
```