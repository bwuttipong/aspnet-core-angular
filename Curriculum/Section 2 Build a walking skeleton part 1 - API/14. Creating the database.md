So the way that we can create our database and we will have a better solution for this soon..

```bash
dotnet ef database update
```
But the SQLite extension is another way to do this. But in order to open our database we need to use the show all commands.

```mardown
shift + P
```

And then we need to look for sqlite and open database inside here. And this wil take a look at your file system. It will recognzie that as a file with a DB. And we can jist select this.

And in order to get these users into our database and by the way, i've not used a Guid here for any of the IDs. Is that going to be a problem? No, it's not because and it's a deliberate choice actually to givee the app user a **string** property rather than a Guid, we could use a Guid.

#### Summarize

In the lecture titled "Creating the database," the instructor explains how to set up a database using a migration file. Here are the key points covered:

    1. Users Table Structure: The primary focus is on creating a users table with three columns: ID (the primary key), display name, and email.

    2. Manual Database Creation: The process for manually creating the database is outlined, using the command line with the 'net f database updates' command, which builds the application and creates the required tables if they don't exist.

    3. Entity Framework and Migration History: The lecture covers how Entity Framework creates tables and maintains migration history to track applied migrations.

    4. Database File Exploration: The instructor emphasizes the importance of having a database file that can be viewed using tools like SQLite viewer and demonstrates how to open the database to inspect the users table.

    5. Populating the Database: The instructor creates three user entries (Bob, Tom, and Jane) with unique IDs, display names, and emails, explaining the choice of using string IDs for easier recognition during development. SQL queries to insert data are demonstrated to confirm the entries.

    6. Simplifying Tools: The lecturer mentions uninstalling an additional SQLite extension to streamline the process, indicating that the SQLite viewer will be the primary tool used.

    7. Preview of Next Topic: The lecture concludes with a preview of the next topic, which involves querying the newly created data through an API endpoint.

If you have any questions or need clarification on any specific part, feel free to ask!
