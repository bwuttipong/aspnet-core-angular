
And then if we want to ensire our packages are restored because I've chagned something inside this file (API.csproj), i do need to run a command. When we install a package using NuGet Gallery, it automatically runs this command. But since i've mad a manual chagne to it. we can execute dotnet restore and that's gonna restore

```bash
dotnet restore
```

i'm gonna go to the extensions tab. And I'm gonna search for SQLite inside here.

```makrdown
SQLite Viewer
``

In this lecture, we discuss the integration of Entity Framework into our project using a SQLite database, which simplifies the development process by eliminating the need for a separate database server.

Here are the key points covered:

    1. Installing Required Packages: We begin by accessing the terminal and ensuring the pre-release option is unchecked in the NuGet Gallery. We install two essential packages: Microsoft.EntityFrameworkCore.Design and Microsoft.EntityFrameworkCore.Sqlite. It's crucial to choose the SQLite package and not the SQLite Core to avoid potential errors.

    2. Verifying Package References: After adding the packages, we review the project file to confirm both Entity Framework packages are included correctly. To restore all packages after making changes, we execute the command dotnet restore. Emphasis is placed on matching the major version of Entity Framework with the .NET runtime version to prevent compatibility issues.

    3. Additional Tools: We then install tools for easier database management, including a SQLite viewer to inspect the database contents, as well as a temporary SQLite tool for querying.

    4. Conclusion and Next Steps: The lecture wraps up with a preview of the next topic, which will involve creating the DbContext class, an essential component for establishing a session with the database.

If you have any questions about integrating Entity Framework or the installation process, feel free to ask!

Was this content relevant to you?
