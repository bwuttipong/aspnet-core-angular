In the lecture titled "Overview of the API Template," the instructor guides us through the API structure created using the .NET framework. Here are the key points covered:

    1. Project Structure: The lecture begins with an explanation of the various folders and files generated by the 'net new' command, emphasizing the importance of understanding the structure for better project management.

    2. Running the Application: You learn how to run the application using the command 'dotnet run,' which starts the application on a random port. The instructor notes that since it’s a web API, no HTML content will display in a browser when accessing the root address.

    3. Launch Settings: The 'launchSettings.json' file is introduced, detailing the two profiles (HTTP and HTTPS) for running the application. The instructor chooses to keep only the HTTPS profile and sets the application to run on port 5001, highlighting the importance of saving changes for proper execution.

    4. API Controllers: The lecture introduces API controllers, focusing on the weather forecast controller that handles HTTP requests and responses. You see how to access the API endpoint and confirm its functionality by retrieving weather forecast data.

    5. Configuration Files: The roles of configuration files like 'appsettings.json' and 'appsettings.Development.json' are discussed, with updates made to the logging level for improved visibility in development.

    6. Startup Class: The startup class is covered, which is essential for registering services and configuring middleware. The instructor advises removing unnecessary components to maintain application simplicity.

    7. HTTPS Certificates: Lastly, the lecture addresses potential issues with HTTPS certificates, providing commands to check, clean, and trust the certificates using the .NET CLI, assuring viewers that this resolves common untrusted certificate problems.

In conclusion, this lecture lays the foundational knowledge for creating and managing an API template, setting the stage for upcoming lessons on building custom classes to handle user-related information. If you have any specific questions or want to discuss any part further, just let me know!


```bash
cd API
dotnet run
```

Inside the folder we've got a propertiess folder.
Inside here is the launch Settings.json and the lauch Settings.json is what Net Run looks at to see what it should use to run the application.

We're gonna to  be running application over Https. So we don't need two profiles in here.
And I'm actually going to remove the first one and just leawve the second profile in there

I'm gonna remove the HTTP one and just use localhost. But I'm gonna change this to 5001. That was traditionally the default port for. Net applications running over Https. So please do stick with Https when you install the. .Net SDK, it should have set up the certificates so that this is going to be trusted by your browser.

Also, what we have inside here is there's an API file (API.http). This's usefull for testing API. I'm not goona use that in this project.

We've also got configuration files where we can supply configuration values to project
So if we want developnet specific settings, they can go inside here. And I'm just going to update this one as i do like to get a little bit more informaion about what's going on as we're building the applcaition

This is our startup class (Program.cs). When we run our application then any services that are dotnet appliction is using are going to be registered inside this file.

And then we've got something called open API. Now I'm goint to get into detauls about what this is because we're going to remove it. It's very useful tool to help us automtically document our API. And we're not goint to cover that in this course.

And we're got Configure the HTTP request pipline session. And this ession is for middleware. And in that pipeline maybe  we'll check to see if authenication is required. Again this is not somthing we're going to use.

We don't need Https redirection because we're just going to run our application over https. And we are gonnacover authorization but not yet. So in terms of the theme of keeping it simple, I'm just gonna remove everything that we're not using.

So just to finish off, i just want to go back to the terminal and talk about what's going on with the Https side of things, just in case you have an issue with this.

So when we installed the .Net SDK, it should've automatically installed and effectively trusted the certificates that it uses for developments. And there's a few commands that we can use to verify and fix this if we have an issue. So using the dotnet cli command we can run dev certs https.
```bash
dotnet dev-certs https -h
clear
dotnet dev-certs https --check
dotnet dev-certs https --clean
dotnet dev-certs https --trust


```